---
- name: Install nginx
  apt:
    name: nginx
    state: present

- name: Remove default nginx site
  file:
    path: /etc/nginx/sites-enabled/default
    state: absent
  notify: reload nginx

# Um arquivo por projeto (vhost_file): outro projeto no mesmo servidor tem o
# seu, na sua própria porta.
- name: Deploy studygo vhost
  template:
    src: "{{ playbook_dir }}/templates/app.conf.j2"
    dest: /etc/nginx/sites-available/{{ vhost_file }}
    mode: '0644'
  notify: reload nginx

- name: Enable studygo vhost
  file:
    src: /etc/nginx/sites-available/{{ vhost_file }}
    dest: /etc/nginx/sites-enabled/{{ vhost_file }}
    state: link
  notify: reload nginx

- name: Ensure nginx is enabled and running
  systemd:
    name: nginx
    enabled: true
    state: started
