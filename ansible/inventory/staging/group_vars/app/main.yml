---
# O ambiente da esteira (push na main). Desde 25/09/2026 roda num servidor
# dentro do WSL da máquina de desenvolvimento — a distro `ubuntu-server` —,
# publicado por um túnel da Cloudflare; a VPS foi suspensa.
#
# O servidor só fica no ar com o PC ligado e a distro de pé.
# Os domínios de caetasousa.tech se foram junto com a VPS. Hoje o nome só
# alimenta o CORS_ORIGIN (a SPA é servida do mesmo endereço que a API, então
# não faz diferença) — o endereço público é o do Quick Tunnel, abaixo, e o
# nginx aceita qualquer nome. Troque aqui quando houver domínio próprio.
app_domain: staging.cronograma.caetasousa.tech
app_domain_www: cronograma.caetasousa.tech
app_dir: /opt/studygo-staging
backend_port: 18080
frontend_port: 15173

postgres_user: studygo_staging
postgres_db: studygo_staging

vhost_file: studygo-staging.conf

# --- servidor no WSL ---------------------------------------------------------
# As distros do WSL2 dividem a mesma rede: um segundo Docker comum brigaria
# com o da distro de desenvolvimento (docker0, iptables), e um ufw fecharia a
# rede das duas. Daí o Docker do usuário de deploy, sem firewall local, e a
# borda pelo cloudflared — nenhuma porta aberta para fora.
docker_rootless: true
deploy_user: annyGo
firewall_local: false
borda: cloudflare
# Onde o nginx escuta e para onde o túnel aponta (painel da Cloudflare:
# Public Hostname → HTTP → localhost:8480).
nginx_listen: 127.0.0.1:8480
# Sem domínio próprio (os de caetasousa.tech se foram com a VPS): o endereço
# público é um *.trycloudflare.com que muda a cada reinício do serviço — veja
# com `make servidor-endereco`. Com um domínio na Cloudflare, aponte-o para o
# túnel do token (cloudflared_token) e desligue isto.
cloudflared_rapido: true
