# Local env files — only *.example is tracked
.env
.env.*
!.env.example

# Editor / OS
.DS_Store
*.swp

# Real edital PDFs — never commit to the public repo (only synthetic fixtures)
*.pdf

# Local design canvas exports — working material, not part of the app
design-directions/

# Per-machine MCP server config
.mcp.json

# local design scratch (canvas artboards)
.design-cronograma/

# Guia de configuração com segredos — local, nunca versionar
GUIA-GITLAB.md
PARTE-7-VARIAVEIS.md
